import { HideForbiddenDirective } from './hide-forbidden.directive';

describe('HideForbiddenDirective', () => {
  it('should create an instance', () => {
    const directive = new HideForbiddenDirective();
    expect(directive).toBeTruthy();
  });
});
