import { DisableForbiddenDirective } from './disable-forbidden.directive';

describe('DisableForbiddenDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableForbiddenDirective();
    expect(directive).toBeTruthy();
  });
});
